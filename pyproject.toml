[project]
name = "real-agent-101"
version = "0.1.0"
description = "Build a Claude Code-like agent from scratch"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "rich>=13.0",
    "python-dotenv>=1.0",
]

[project.scripts]
agent = "src.main:main"
